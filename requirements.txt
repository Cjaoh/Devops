# =============================================================
# requirements.txt — mis à jour le 2026-04-01
# Correctifs CVE : python-jose, python-multipart, starlette
# =============================================================

# --- Web framework ---
fastapi==0.115.12
uvicorn[standard]==0.32.1
python-multipart==0.0.22

# --- Base de données ---
sqlalchemy==2.0.36
asyncpg==0.30.0
greenlet==3.1.1

# --- Authentification JWT ---
# python-jose 3.3.0 → CVE-2024-33663 CRITICAL (algorithm confusion)
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# --- Validation ---
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# --- HTTP client ---
httpx==0.27.2

# --- Utilitaires ---
python-dotenv==1.0.1
psycopg2-binary==2.9.9
