# =============================================================
# requirements.txt — Dépendances de l'application
# Testé avec Python 3.11 / 3.12 / 3.13
# =============================================================

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.18

# --- Base de données (SQLAlchemy async + PostgreSQL) ---
sqlalchemy==2.0.36
asyncpg==0.30.0
greenlet==3.1.1

# --- Authentification JWT ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# --- Validation / Sérialisation ---
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# --- HTTP client (pour tests) ---
httpx==0.27.2

# --- Utilitaires ---
python-dotenv==1.0.1
